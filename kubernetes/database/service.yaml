apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  labels:
    app: database
spec:
  type: ClusterIP
  selector:
    app: database
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
